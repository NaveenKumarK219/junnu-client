import { SearchNamesPipePipe } from './search-names-pipe.pipe';

describe('SearchNamesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchNamesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
